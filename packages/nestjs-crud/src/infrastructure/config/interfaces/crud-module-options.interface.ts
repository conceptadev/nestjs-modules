import { type CrudModuleSettingsInterface } from './crud-module-settings.interface.js';

export interface CrudModuleOptionsInterface {
  /**
   * Module settings.
   */
  settings?: CrudModuleSettingsInterface;
}
