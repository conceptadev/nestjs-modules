/** Default take limit when none specified. */
export const FEDERATION_DEFAULT_LIMIT = 10;

/** Maximum iterations for iterative constraint discovery. */
export const FEDERATION_MAX_ITERATIONS = 10;

/** Maximum offset before aborting iterative discovery. */
export const FEDERATION_MAX_BUFFER_SIZE = 1000;
