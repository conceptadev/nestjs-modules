/**
 * Role module registration options. Currently empty — the v7-era
 * `settings.assignments` map was superseded by `RoleModule.forFeature`'s
 * `assignmentEntityKeys`. Kept as an extension point for genuinely
 * module-wide options, should one ever be needed.
 */
export interface RoleOptionsInterface {}
